const kelvin = 0
// To start, create a variable named kelvin, and set it equal tok;kjlj 
const celsius = kelvin - 273;
// Let’s convert Kelvin to Celsius by subtracting 273 from the 
let fahrenheit = celsius * (9/5) + 32;
// Use this equation to calculate Fahrenheit, then store the 
fahrenheit = Math.floor(fahrenheit);
// Use the .floor() method from the Math library to round down
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);
// convert to newton
let newton = celsius * (33/100);
// round down
newton = Math.floor(newton);
console.log(`The temperature is ${newton} degrees Newton.`);

